const pathTo = x => require("../../../" + x);
const lib = pathTo("lib/immutable/set").lib;

describe("set", () => {
  let object, value;
  beforeEach(() => (object = {}));
  beforeEach(() => (value = 1));
});
