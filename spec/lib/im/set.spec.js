const pathTo = x => require("../../../" + x);
const lib = pathTo("lib/im/set").lib;

describe("set", () => {
  let object, value;
  beforeEach(() => (object = {}));
  beforeEach(() => (value = 1));
});
